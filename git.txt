git config --global user.name "John Doe"
git config --global user.email johndoe@example.com
// Staging files
git add filename
git add --all
git add -A
git add .
git commit -m "comment"

.gitignoore
.DS_store
.vscode/
authentication.js
mode_modules
notes/
**/*-todo.md



git add .
git commit -m ".gitignore"

git rm file
git mv file newfile


git branch
git status
git -c switch new-branch

git merge branch
git switch main
git merge new-branch

for deleting the branch
git -d branch-name


creating new branches
git branch alpha-branch
git checkout alpha branch



github
git remote add origin https://github.com/diypa571/
git branch -M main
git push -u origin main


https://github.com/diypa571/git.git
